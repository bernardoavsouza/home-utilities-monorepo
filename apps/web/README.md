# apps/web

Next.js App Router. Setup and scripts live in the [repo root README](../../README.md) (`pnpm bootstrap`, `pnpm dev:web`).

From this package: `pnpm --filter web dev`. pnpm only — npm/yarn are blocked at the workspace root.

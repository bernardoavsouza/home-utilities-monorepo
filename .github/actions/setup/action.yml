name: Setup workspace
description: pnpm from the pinned version, Node from .nvmrc, and a frozen install.

runs:
  using: composite
  steps:
    - uses: pnpm/action-setup@v5
      with:
        version: 11.20.0

    - uses: actions/setup-node@v5
      with:
        node-version-file: .nvmrc
        cache: pnpm

    - name: Install
      run: pnpm install --frozen-lockfile
      shell: bash
